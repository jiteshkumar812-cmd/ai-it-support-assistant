apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: aks-apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: acraiitdev001.azurecr.io/frontend:latest
        ports:
        - containerPort: 80
